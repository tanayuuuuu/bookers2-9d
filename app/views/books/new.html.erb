<h1>画像投稿フォーム</h1>
  <%= form_with model: @book do |f| %>
    <h4>Title</h4>
      <%= f.file_area :title %>
    <h4>Opinion</h4>
      <%= f.text_field :body %>
    <%= f.submit 'Create Book' %>
  <% end %>