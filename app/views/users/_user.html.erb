
    <div class="col-md-3">
      <h2>User info</h2>
      <%= image_tag user.get_profile_image(100,100) %>
      <table class="table">
        <tbody>
          <tr></tr>
          <tr>
            <th>name</th>
            <th><%= current_user.name %></th>
          </tr>
          <tr>
            <th>introduction</th>
          <th></th>
          </tr>
        </tbody>
      </table>
      <div class="row">
        <%= link_to edit_user_path(current_user),class: "btn btn-outline-secondary btn-block edit_user_11572" do %>
          <i class="fas fa-user-cog"></i>
        <% end %>
      </div>

      <h2 class="mt-3">New Book</h2>
        <div class="form-group">
          <lavel for="book_title">Title</lavel>
          <%= form_with model: book do |f| %>
          <%= f.text_field :title %>
        </div>

        <div class="form-group">
          <lavel for="book_opinion">Opinion</lavel></br>
          <%= f.text_area :body %></br>
          <%= f.submit "Create Book",class: "btn btn-success" %>
          <% end %>
        </div>
    </div>