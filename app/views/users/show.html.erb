<%= render "user", user: @user, book: @book %>


<h1>Books</h1>
<% @books.each do |book| %>
<%= image_tag book.user.get_profile_image(100,100) %>
  <p>Title</p>
  <span><%= book.title %></span>

  <P>Opinion</P>
  <span><%= book.body %></span>
<% end %>